[package]
name = "whitelist-hide-runtime"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
whitelist-hide-core = { path = "../core" }

[lints]
workspace = true
