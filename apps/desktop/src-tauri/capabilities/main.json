{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal desktop capability. No shell or filesystem plugin permissions.",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": [
    "core:app:default",
    "core:event:default",
    "core:window:default"
  ]
}
